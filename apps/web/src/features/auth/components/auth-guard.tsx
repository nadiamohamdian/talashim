'use client';

/**
 * @deprecated Use route-group `(protected)/layout` + middleware instead.
 * Kept for gradual migration of any legacy imports.
 */
export { ProtectedShell as AuthGuard } from './protected-shell';
