# Sadaf Gold — Admin

پنل مدیریت — پورت **3002** (جدا از فروشگاه)

```bash
pnpm --filter @sadafgold/admin dev
# http://localhost:3002/login
```

## فروشگاه مشتری

**http://localhost:3001** — خانه و محصولات بدون ورود

```bash
pnpm --filter @sadafgold/web dev
```
