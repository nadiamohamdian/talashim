{
  "name": "talashim",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20.9.0",
    "pnpm": ">=9.12.0"
  },
  "scripts": {
    "dev": "turbo run dev --parallel --continue",
    "dev:infra": "docker compose up postgres redis minio -d",
    "dev:db": "pnpm --filter @sadafgold/api prisma:migrate:deploy && pnpm --filter @sadafgold/api prisma:seed",
    "dev:web": "pnpm --filter @talashim/web dev",
    "dev:store": "pnpm --filter @talashim/web dev",
    "dev:admin": "pnpm --filter @sadafgold/admin dev",
    "dev:api": "pnpm --filter @sadafgold/api dev",
    "smoke:roles": "pnpm --filter @sadafgold/api smoke:roles",
    "dev:worker": "pnpm --filter @sadafgold/worker dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "clean": "rimraf .turbo \"apps/*/.next\" \"apps/*/dist\" \"packages/*/dist\"",
    "docker:dev": "docker compose up --build",
    "docker:prod": "docker compose -f docker-compose.prod.yml --env-file .env.production up --build -d",
    "docker:prod:ssl": "docker compose -f docker-compose.prod.yml -f docker-compose.ssl.yml --env-file .env.production up -d",
    "deploy": "bash scripts/deploy.sh",
    "deploy:health": "bash scripts/health-check.sh"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.4",
    "@tailwindcss/postcss": "catalog:",
    "@types/node": "catalog:",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "eslint": "catalog:",
    "eslint-config-next": "catalog:",
    "eslint-config-prettier": "^10.1.5",
    "globals": "^17.0.0",
    "prettier": "catalog:",
    "rimraf": "^6.0.1",
    "tailwindcss": "catalog:",
    "tsx": "^4.22.3",
    "turbo": "catalog:",
    "typescript": "catalog:",
    "typescript-eslint": "catalog:"
  },
  "dependencies": {
    "@tanstack/react-query": "catalog:"
  }
}
