# Legacy static config — production uses envsubst templates:
#   docker/nginx/templates/http/10-http.conf.template  (HTTP)
#   docker/nginx/templates/ssl/20-https.conf.template  (HTTPS + docker-compose.ssl.yml)
# See README.md → Production deployment.
